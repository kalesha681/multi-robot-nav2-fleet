# ==============================================================================
# Multi-Robot Nav2 Fleet - System & Python Dependencies
# Target OS: Ubuntu 22.04 LTS (Jammy)
# Target ROS: ROS 2 Humble Hawksbill
# Target Simulator: Gazebo Sim (Harmonic / Fortress via ros_gz)
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. CORE PYTHON DEPENDENCIES (Install via: pip install -r requirements.txt)
# ------------------------------------------------------------------------------
numpy>=1.21.5
pyyaml>=5.4.1
transforms3d>=0.4.1
matplotlib>=3.5.0
scipy>=1.8.0
flake8>=4.0.1
pytest>=7.0.0
colcon-common-extensions>=0.3.0
rosdep>=0.22.0
vcstool>=0.3.0

# ------------------------------------------------------------------------------
# 2. SYSTEM & ROS 2 PACKAGES (Install via: sudo apt-get install -y <packages>)
# ------------------------------------------------------------------------------
# [Gazebo Harmonic (Version 8.x) & ROS 2 Bridges]
# gz-harmonic
# ros-humble-ros-gzharmonic
# ros-humble-ros-gzharmonic-sim
# ros-humble-ros-gzharmonic-bridge
# ros-humble-ros-gzharmonic-interfaces
# ros-humble-ros-gzharmonic-image
# ros-humble-actuator-msgs

# [Nav2 & MPPI Controller Ecosystem]
# ros-humble-navigation2
# ros-humble-nav2-bringup
# ros-humble-nav2-mppi-controller
# ros-humble-nav2-smac-planner
# ros-humble-nav2-navfn-planner
# ros-humble-nav2-smoother
# ros-humble-nav2-costmap-2d
# ros-humble-nav2-behavior-tree
# ros-humble-nav2-bt-navigator
# ros-humble-nav2-controller
# ros-humble-nav2-planner
# ros-humble-nav2-behaviors
# ros-humble-nav2-waypoint-follower
# ros-humble-nav2-velocity-smoother

# [Mapping & SLAM]
# ros-humble-slam-toolbox

# [Robot Modeling, State & Transforms]
# ros-humble-robot-state-publisher
# ros-humble-joint-state-publisher
# ros-humble-joint-state-publisher-gui
# ros-humble-xacro
# ros-humble-tf2-ros
# ros-humble-tf2-geometry-msgs
# ros-humble-tf2-sensor-msgs

# [Control, Multiplexing & Telemetry]
# ros-humble-twist-mux
# ros-humble-teleop-twist-keyboard
# ros-humble-rviz2
# ros-humble-rosbag2
# ros-humble-rosbag2-storage-mcap

# ------------------------------------------------------------------------------
# 3. ONE-STEP ROSDEP INSTALLATION COMMAND
# ------------------------------------------------------------------------------
# Run from workspace root (~/AMR_ws):
#   sudo apt update
#   sudo rosdep init 2>/dev/null || true
#   rosdep update
#   rosdep install --from-paths src --ignore-src -r -y
